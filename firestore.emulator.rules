rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // En desarrollo/emuladores: permitir todo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}