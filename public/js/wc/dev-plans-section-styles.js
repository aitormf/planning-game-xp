/**
 * Styles for DevPlansSection component
 */
import { css } from 'https://cdn.jsdelivr.net/npm/lit@3.0.2/+esm';

export const DevPlansSectionStyles = css`
  :host {
    display: block;
    font-family: var(--font-family-base, 'Segoe UI', system-ui, sans-serif);
  }

  .dev-plans-section {
    padding: 0.5rem 0;
  }
`;
